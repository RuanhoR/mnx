{
  "name": "pmnx-server",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },

  "scripts": {
    "deploy": "node scripts/build.js cloudflare",
    "cf:build": "node scripts/build.js cloudflare",
    "node:build": "node scripts/build.js nodejs",
    "dev": "pnpm wrangler dev",
    "start": "pnpm wrangler dev",
    "test": "vitest",
    "cf-typegen": "pnpm wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.4",
    "@types/node": "^25.6.0",
    "supabase": "^2.92.1",
    "typescript": "^5.5.2",
    "vitest": "~3.2.0",
    "wrangler": "^4.83.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.103.3"
  }
}
